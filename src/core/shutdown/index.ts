/**
 * Shutdown module exports
 *
 * Provides graceful shutdown functionality for clean application termination.
 */

export { ShutdownManager, createShutdownConfig } from './shutdown';
export type { ShutdownConfig } from './shutdown';
