-- This migration intentionally contains no schema changes.
-- It exists to synchronize Drizzle metadata snapshot history with
-- the current committed schema state after legacy snapshot drift.
SELECT 1;
